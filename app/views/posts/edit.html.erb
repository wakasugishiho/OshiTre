<%= render partial: "homes/header" %>

<div class="container">
	<div class="row">
		<h3>記事編集</h3>
	</div>
	<div class="row">
		<%= form_for @post do |f| %>
			<%= f.label "タイトル" %>
			<%= f.text_field :title %>
			<%= f.label "画像" %>
			<%= f.attachment_field :image %>
			<%= f.label "本文" %>
			<%= f.text_area :body %>
			<%= f.submit "記事を編集する" %>
		<% end %>
	</div>
	<div class="row">
		<h3>タグを変更する</h3>
	</div>
</div>